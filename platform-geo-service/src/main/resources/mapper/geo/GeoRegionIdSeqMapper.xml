<?xml version="1.0" encoding="UTF-8"?>
<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN"
        "https://mybatis.org/dtd/mybatis-3-mapper.dtd">
<mapper namespace="com.caopan.platform.geo.mapper.GeoRegionIdSeqMapper">

    <!-- next_id 存「下一个待分配」；LAST_INSERT_ID(next_id) 捕获当前值后再 +1 -->
    <update id="bumpAndCapture">
        UPDATE geo_region_id_seq
        SET next_id = LAST_INSERT_ID(next_id) + 1
        WHERE level = #{level}
    </update>

    <select id="lastInsertId" resultType="long">
        SELECT LAST_INSERT_ID()
    </select>

    <insert id="upsertFloor">
        INSERT INTO geo_region_id_seq (level, next_id)
        VALUES (#{level}, #{floorNextId})
        ON DUPLICATE KEY UPDATE next_id = GREATEST(next_id, VALUES(next_id))
    </insert>
</mapper>
